#pragma once
#include <stddef.h>
#include "LinkedList.h"

/**
 * Parses a csv file and outputs a LinkedList with each line
 * as a node.
 * @param filename The input csv file.
 * @returns A linked list of each line as a node.
 */
LinkedList parseFile( const char* filename )
{
  return NULL;
}
